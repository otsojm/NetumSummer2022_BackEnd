/*DROP TABLE persons;

CREATE TABLE persons (person_id SERIAL PRIMARY KEY NOT NULL, first_name varchar(255) NOT NULL, last_name varchar(255) NOT NULL,  age integer NOT NULL);

INSERT INTO persons (first_name, last_name, age)
VALUES ('Otso', 'Metsaportti', 23);
*/
